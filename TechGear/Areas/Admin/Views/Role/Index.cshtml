@* TechGear MVC no longer uses Identity - User management is handled by API *@
@* This view is disabled *@

@*
@using TechGear.Models
@model IEnumerable<ApplicationUser>

<h2>User Permission Management</h2>

<table class="table">
    <tr>
        <th>Email</th>
        <th>Action</th>
    </tr>
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Email</td>
            <td>
                <a asp-action="Edit" asp-route-id="@user.Id">Change Role</a>
            </td>
        </tr>
    }
</table>
*@

<div class="alert alert-warning">
    <h4>Identity Disabled</h4>
    <p>TechGear MVC no longer uses ASP.NET Core Identity. User management should be handled through the API.</p>
</div>
